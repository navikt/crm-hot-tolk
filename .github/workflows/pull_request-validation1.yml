name: "[PR] Validate"
on:
  pull_request:
    branches:
      - "*"
jobs:
  run-pr-validation:
    name: Validate PR
    uses: navikt/crm-workflows-base/.github/workflows/validate.yml@test_node_downgrade
    permissions:
      contents: read
    secrets: inherit
