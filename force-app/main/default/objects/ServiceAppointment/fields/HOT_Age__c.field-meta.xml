<?xml version="1.0" encoding="UTF-8"?>
<CustomField xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>HOT_Age__c</fullName>
    <externalId>false</externalId>
    <formula>VALUE(
TRIM(
LEFT(
RIGHT(
HOT_GenderAge__c, LEN(HOT_GenderAge__c) - FIND(&apos; &apos;, HOT_GenderAge__c))
,
FIND(&apos; &apos;, RIGHT(HOT_GenderAge__c, LEN(HOT_GenderAge__c) - FIND(&apos; &apos;, HOT_GenderAge__c)))
)
)
)</formula>
    <formulaTreatBlanksAs>BlankAsZero</formulaTreatBlanksAs>
    <label>Age</label>
    <precision>18</precision>
    <required>false</required>
    <scale>0</scale>
    <trackHistory>false</trackHistory>
    <type>Number</type>
    <unique>false</unique>
</CustomField>
