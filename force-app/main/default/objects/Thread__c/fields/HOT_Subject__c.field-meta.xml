<?xml version="1.0" encoding="UTF-8"?>
<CustomField xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>HOT_Subject__c</fullName>
    <externalId>false</externalId>
    <formula>IF(NOT(ISBLANK(HOT_InterestedResource__c)),
    HOT_InterestedResource__r.ServiceAppointmentSubject__c,
    IF(NOT(ISBLANK(HOT_ServiceAppointment__c)),
        HOT_ServiceAppointment__r.Subject,
        IF(NOT(ISBLANK(HOT_Request__c)),
            HOT_Request__r.Subject__c,
            IF(NOT(ISBLANK(HOT_WorkOrder__c)),
                HOT_WorkOrder__r.Subject,
                IF(NOT(ISBLANK(HOT_WageClaim__c)),
                    IF(NOT(ISBLANK(HOT_WageClaim__c)),
                        HOT_WageClaim__r.ServiceAppointment__r.Subject,
                        null
                    ),
                    null
                )
            )
        )
    )
)</formula>
    <formulaTreatBlanksAs>BlankAsZero</formulaTreatBlanksAs>
    <label>Subject</label>
    <required>false</required>
    <trackHistory>false</trackHistory>
    <trackTrending>false</trackTrending>
    <type>Text</type>
    <unique>false</unique>
</CustomField>
